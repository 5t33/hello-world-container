{
  "name": "hello-world-container",
  "version": "1.0.0",
  "description": "A simple container for experimenting with new architecture",
  "main": "index.js",
  "scripts": {
    "start": "node dist/src/app.js",
    "develop": "npm run build && node dist/src/app.js",
    "test": "jest test",
    "smoke-tests": "jest smoke-tests",
    "build": "./node_modules/typescript/bin/tsc",
    "build-docker": "npm run build && docker build ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.6",
    "@types/jest": "^26.0.19",
    "jest": "^26.6.3",
    "typescript": "^3.9.5"
  }
}
